This repository contains my web development assignment files created as part of the Web Development Lab at K.R. Mangalam University.
how i created this 
1.	Created an HTML file using Notepad and wrote the basic structure of the webpage using HTML tags such as <html>, <head>, <body>, <h1>, <p>, etc.
2.	Created a separate CSS file to style the webpage. In the CSS file, I added properties like color, background-color, margin, padding, and border.
3.	Linked the CSS file to the HTML file by adding this line inside the <head> section of the HTML:
